#include <bits/stdc++.h>

using namespace std;

int I[4];

int main()
{
	for (int i = 0; i < 4; i++)
		I["UHNV"[i]] = i;

	vector<vector<int>> A(4, vector<int>(4, INT_MAX)), B = A;
	A[1][3] = 0;

	int n;
	cin >> n;
	string s;
	int	   ans = 0;
	while (n--)
	{
		cin >> s;
		if (size(s) == 1)
		{
			for (int )
		}
	}
}
