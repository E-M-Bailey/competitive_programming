#include "template.hpp"

using namespace std;
using namespace abbrev;

int main()
{
	cin.tie(0)->sync_with_stdio(0);
	int T;
	cin >> T;
	while (T--)
	{
		int n, m;
		cin >> n >> m;
		while (m--)
		{
			int a, b;
			cin >> a >> b;
		}
		cout << n - 1 << '\n';
	}
}
