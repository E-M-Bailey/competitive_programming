#include <bits/stdc++.h>

using namespace std;

typedef double F;

int N, K, T, R;



int main(int argc, char const *argv[])
{
	if (argc > 0)
	{
		ignore = freopen(argv[1], "r", stdin);
		ignore = freopen(argv[2], "r", stdin);
	}
	cin.tie(0)->sync_with_stdio(0);


}

